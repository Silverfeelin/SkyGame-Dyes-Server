{
  "name": "durable-object-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --persist-to ./.wrangler-shared",
    "start": "wrangler dev --persist-to ./.wrangler-shared",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.3",
    "typescript": "^5.5.2",
    "wrangler": "^3.106.0"
  }
}
